<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Use relative paths for resources -->
    <link rel="stylesheet" href="/public/css/index.css">
    <script src="/public/js/index.js" defer></script>
</head>

<body>
    <h1>URL Shortener</h1>
    <!-- Use action with relative path -->
    <form action="/short/shorten" method="POST">
        <input type="text" name="url" id="url" placeholder="Enter your URL here">
        <label for="url">URL</label>
        <button type="submit">Shorten</button>
    </form>
    <div id="result" class="hidden">
        <div id="success-message">URL shortened successfully!</div>
        <p>Your shortened URL is ready:</p>
        <p><a href="#" id="shortUrl" target="_blank">shortUrl</a></p>
        <div class="button-container">
            <button type="button">Copy URL</button>
            <button type="button">Close</button>
        </div>
    </div>
</body>

</html>